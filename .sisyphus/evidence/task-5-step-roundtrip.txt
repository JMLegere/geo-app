T5: Step persistence SQLite round-trip evidence
================================================
Date: 2026-03-10
Test file: test/core/persistence/profile_repository_test.dart

Tests added:
  1. "step fields round-trip through SQLite"
     - create(totalSteps: 1500, lastKnownStepCount: 42000)
     - read → assert totalSteps == 1500, lastKnownStepCount == 42000
     - update(totalSteps: 2000) → read → assert totalSteps == 2000, lastKnownStepCount == 42000 (unchanged)
     - update(lastKnownStepCount: 55000) → read → assert totalSteps == 2000, lastKnownStepCount == 55000

  2. "step fields default to zero when not provided"
     - create() with no step fields → read → totalSteps == 0, lastKnownStepCount == 0

  3. "update preserves step fields when not specified"
     - create(totalSteps: 500, lastKnownStepCount: 10000)
     - update(currentStreak: 3) — no step params
     - read → totalSteps == 500, lastKnownStepCount == 10000 (preserved), currentStreak == 3

Test run result: 58 tests passed (0 failures)
Command: LD_LIBRARY_PATH=. flutter test test/core/persistence/

Changes made:
  - ProfileRepository.update() now accepts optional totalSteps and lastKnownStepCount params
  - Both fields included in copyWith() chain using ?? existing.field pattern
  - ProfileRepository.create() already had these params (from T3 migration)
