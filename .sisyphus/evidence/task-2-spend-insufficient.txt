# Task 2: PlayerNotifier.spendSteps() — Insufficient Balance Test

## Test Scenario
Spend 500 steps with balance 499 → returns false, balance unchanged (499)

## Test Output
00:00 +12: PlayerNotifier spendSteps returns false and does not mutate state when amount > totalSteps

## Verification
✓ Test 12: Insufficient balance (499 → spend 500 → 499, no mutation) PASS
✓ Test 14: Zero spend (1000 → spend 0 → 1000, returns false) PASS
✓ Test 15: Negative spend (1000 → spend -100 → 1000, returns false) PASS

## Edge Cases Tested
1. amount > totalSteps → false, no mutation ✓
2. amount == 0 → false, no mutation ✓
3. amount < 0 → false, no mutation ✓
4. amount == totalSteps → true, balance becomes 0 ✓

## Implementation Details
- Guard clause prevents overdraft: `if (amount <= 0 || amount > state.totalSteps) return false;`
- No state mutation on failure
- Immutable update via copyWith() on success

## Status
✓ COMPLETE — All validation tests passing
