# Cold-start reproducibility is a repository invariant

EarthNova must enable a fresh agent with no prior conversation or private memory to take one human-approved outcome from repository discovery through implementation and user-level verification using only the repository and its isolated development environment. We accept the maintenance cost of self-contained repository state because reliable handoffs, parallel execution, recovery, and agent-vendor independence are more valuable than relying on hidden session continuity.
