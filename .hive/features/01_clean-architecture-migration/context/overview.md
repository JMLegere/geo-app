## At a Glance
Migrate flat `lib/{models,services,providers,screens,widgets}/` to Clean Architecture with feature modules. TDD: every new file gets a failing test first. 25 source files, 17 test files.

## Workstreams
1. Core domain entities + DTOs + extensions (TDD — additive only, nothing breaks)
2. Auth feature — repositories, use cases, presentation (TDD)
3. Identification feature + full switchover + test migration (TDD)
4. Update AGENTS.md + solution diagrams

## Revision History
- 2026-04-06: Initial plan written, TDD approach
