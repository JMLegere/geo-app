{
  "name": "earthnova-e2e",
  "private": true,
  "scripts": {
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed",
    "test:debug": "npx playwright test --debug",
    "serve": "npx serve ../build/web -l 8080 -s"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.0",
    "serve": "^14.0.0"
  }
}
